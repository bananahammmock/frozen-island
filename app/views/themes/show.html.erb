<h1>QUOTES, YO.</h1>
<br>
<% if flash[:notice] %>
    <div class='alert alert-info'><%= flash[:notice] %></div>
  	<% end %>
<br>
<% if @theme.id == 6 %>
<h3>
	<% if @current_subs.count == 0 %>
	<div class="btn-group btn-group-lg">
		<button type="button" class="btn btn-default">
		<%= link_to "Subscribe", new_subscription_path(:user_id => current_user.id, :theme_id => @theme.id )%>
		</button>
	<% else %>
		<%= button_to "UnSubscribe", subscription_path(:user_id => current_user.id, :theme_id => @theme.id), :method => :delete %>
	<% end %>

</h3>
	<br>
	<br>
	<h2>Added Quotes</h2>
	<% @theme.inspirations.each do |i| %>
		<%= i.quote %>
		<br>
	<% end %>
	<h2>All Quotes</h2>
		<% @bob.each do |b| %>
			<%= b.quote %>
		<small><%= link_to "Add to Custom Subscription", new_inspiration_path(:quote => b.quote) %></small>
		<br>
		<% end %>
		<br>
		<br>
blargh
<% else %>
<h3>
<% if @current_subs.count == 0 %>
	<div class="btn-group btn-group-lg">
	<button type="button" class="btn btn-default">
	<%= link_to "Subscribe", new_subscription_path(:user_id => current_user.id, :theme_id => @theme.id )%>
	</button>
<% else %>
	<%= button_to "UnSubscribe", subscription_path(:user_id => current_user.id, :theme_id => @theme.id), :method => :delete %>
	
<% end %>
</h3>
<br>
<% @theme.inspirations.each do |i| %>
	<%= i.quote %>
	<br>
<% end %>
<% end %>
<br>
<%= link_to "Back to Main", root_path %>